name: hello-world-action

on:
  push:
    branches:
      - main

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting
        run: echo "Hello World - GitHub Actions working!"
